# 开发与测试日志

> 用途：记录开发与测试过程中的关键日志——做了什么、遇到什么 Bug、怎么修的、测试过程与结果。
> 供定期检查时快速了解工作进展。**新条目追加在最上方**。

## 条目模板

```markdown
## [YYYY-MM-DD] 一句话标题

**做了什么**：...
**Bug**：
- [BUG-xxx] 现象 → 原因 → 修复
**测试**：...
**结果**：...
**待办/下一步**：...
```

## [2026-08-21] metric-recall：完成无 embedding 指标候选召回方案设计

**做了什么**：
- 归档 `query-capability` 并将 delta specs 同步到主规格；随后在 `codex/metric-recall` 完成 C3 proposal/design/specs/tasks。
- 设计 LLM 前置 `MetricCandidateRetriever`：精确名称/metricCode/最长别名 pinned + 中文字符 n-gram Top-K；多指标显式命中不受 K 截断。
- 定义低信号/异常/full 模式的完整 catalog 回退，以及 strict Recall@K / effective recall / fallback 三分口径。
- 明确本轮不依赖 embedding；N=61 离线召回为硬门禁，真实 LLM full/topk A/B 为方向性观测。
- 识别并规划修复运行时从 `app/eval/aliases.yaml` 读取别名的依赖倒置：迁移为 memory/recall/eval 共用的运行时唯一数据源。

**Bug**：
- 无实现 Bug；本轮为方案与契约阶段。

**测试**：
- `openspec validate --all --strict --json`：归档前 7/7 主规格有效。
- `openspec validate metric-recall --type change --strict --json`：1/1 change 有效，0 issues。
- `git diff --check`：通过。

**结果**：
- C2 已归档提交；C3 的 4/4 planning artifacts 完成，形成 16 项可执行任务。
- 方案在 embedding 额度不足时仍可完整开发与验收，并以 full catalog 回退守住正确性边界。

**待办/下一步**：
- 等待 review `metric-recall`；吸收 review 后 apply，先完成别名迁移和离线 Recall@K 门禁，再接入真实语义链路。

## [2026-08-21] query-capability：指标值过滤与冲突多指标合成

**做了什么**：
- 实现 change `query-capability` 的语义契约扩展：`filters[].op` 支持 `>`、`>=`、`<`、`<=`；指标 code 过滤进入 `HAVING`，维度过滤仍进入 `WHERE`。
- 冲突多指标统一走子查询 JOIN：来源冲突或同源 fact 的 `eventFilter` 冲突时，各指标独立聚合后按共享维度键对齐；同源同 filter 继续单 FROM 多列。
- n02「各分类的完播率和互动率」从 fallback 解锁为 semantic；新增 4 个指标值过滤 golden case，并记录独立手工 SQL truth source。

**Bug**：
- [修复] C2 扩展后 `test_synonym_cases` 的 cases 数量断言仍固定为 57；更新为 61，并保留原 57 条子集口径。
- [修复] prompt 未明确指标值过滤对应聚合后的 `HAVING`；补充规则与示例，避免模型按普通维度 WHERE 解析。

**测试**：
- Python pytest **186/186**，ruff clean。
- 真实评测：`--llm real --platform mock --memory off`，N=61；可用性 61/61、端到端 60/61、L1 47/49、R1 **29/29**。
- 新增 4 条指标过滤用例：semantic 4/4、R1 4/4；n02 生成跨源子查询 JOIN。评测未启用 embedding。

**结果**：
- 生产常见的“聚合指标超过阈值”已从 raw/fallback 进入确定性 SQL 合成链路；跨源多指标也具备真实 SQL 形态与结果级回归证据。
- 原有 57 条端到端 56/57，唯一失败为 c07 报告字段缺失；原有 L1 单轮 43/45，较上一轮 44/45 的差异标注为 LLM 解析方差，不归因于 C2。

**待办/下一步**：
- 完成 OpenSpec 4.2/4.3/5.1 勾选、最终 diff 检查与提交；用户自行 push/merge。

## [2026-08-20] scale-data：数据模型规模化（指标7→15/表12→16/真实业务模式）

**做了什么**：
- 实现 change `scale-data`（分支 `codex/scale-data`）：
  - **schema.sql +4 表**（creator_revenue/video_revenue/user_retention/content_quality）；TableSchemaRegistry 自动识别 16 表（schema.sql 唯一源）；TableType 新 FACT 表。
  - **DataInitializer 增量灌新表**：既有 2023-10 字节级不变（P1），新增 10+11 月数据；真实业务模式（长尾 80/20、稀疏、异常峰值/断档、复用 10/1-7 节日）。
  - **指标 7→15**：比率（comment_rate/like_rate/share_rate）、跨表收益（creator_revenue/video_revenue）、去重计数（active_creator_count/daily_active_users）、平均完播率。
  - **合成器新表字段映射**（_field_expr/_ALIAS）+ 新指标别名（aliases 28 个）。
  - **评测扩展**：cases 45→57（+12 新用例），8 个加 R1 真值。

**Bug**：
- [修复] video_revenue/avg_completion_ratio 设了 factFormula → `_resolve_path` 误路由到 user_behavior_fact（应走自己 sourceTable）；修 catalog factFormula→None。
- [修复] 别名"活跃创作者数"与 catalog 精确名冲突 → 移除；test_synonym_cases 数量断言 45→57。

**测试**：
- Python pytest **175/175**（+3 合成器单测：比率/去重/新表）、ruff clean；Java mvn compile。
- **新基线（N=57，--memory off）**：可用性 100%；既有 45 L1 96.97%（零回退）、R1 16-17/17（n16 解析方差）；新增 12 **L1 100%、R1 8/8**（新指标/新表全部正确）。

**结果**：
- 规模扩展成功：新指标/新表可查可测（比率/收益/去重全部正确合成 + R1 通过）。
- **规模副作用可测**：catalog 扩大 → prompt 变长 → 既有用例 L1 单轮波动——验证"全量 prompt 不稳定"真实发生，为 schema linking（C3）提供基线。

**待办/下一步**：
- C2（能力补全：数值过滤/跨源多指标，n02 解锁）。
- C3（metric-recall schema linking，规模已到位、有基线对比）。
- 分支 `codex/scale-data` 待 push + PR；之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-19] relative-time-synthesis：R1 驱动的相对时间语义修复（17/17）

**做了什么**：
- 实现 change `relative-time-synthesis`（分支 `codex/relative-time-synthesis`）：
  - **time_expand 纯函数**（`app/synthesis/time_expand.py`）：relative {amount, unit} + 数据末日锚点 → absolute（**含端点**，unit day/week、month 近似 30 天标注）。
  - **nodes 合成前展开**（`_expand_relative_time`）：锚点 = 数据末日（real 走 `SELECT MAX(timeField)` 同源查询，P2-1 用 `_resolve_path` 后 source 的列；mock 固定 2023-10-31）；失败降级 + warning。
  - **R1 扩展**：5 个 relative 用例标 expected_result（c03/c13 trend、n04 exact、n09/n10 top_set）；n11/n22/n23/n25 语义模糊只验 SQL 形态。

**Bug**：
- [修复] `_expand_relative_time` 传整个 time_range（{type, relative, granularity}）给 time_expand 而非内层 {amount, unit} → amount 解析为 1 天；改为 `relative.get("relative") or relative`。
- [修复] time_expand 返回格式为顶层 start/end，而合成器期望 `absolute: {start, end}` 嵌套 → 修正为嵌套格式（对齐 golden c08）。

**测试**：
- Python pytest **172/172**（新增 time_expand 7 + relative_time 5）、ruff clean。
- **评测（--llm real --platform mock，45 用例）**：可用性 100%、L1 96.97%（32/33，零回退）、**R1 17/17（100%）**、交叉诊断空。
- **修复闭环证据**：c03 修复前合成 SQL 无时间过滤（返回 31 天），修复后含 `WHERE date BETWEEN '2023-10-25' AND '2023-10-31 23:59:59'` 且 R1 通过——R1 从 12/12 扩展到 17/17。

**结果**：
- R1 驱动修复闭环完整落地：评测抓到 bug → 修复 → R1 验证修复。
- 简历叙事：结果级评测不是摆设——它上线即抓住真实缺陷并驱动了语义修复。

**待办/下一步**：
- 真实部署时锚点从"数据末日"切换为"系统当前日期"（Open Question 记录）。
- n11 互动率公式的真值断言（复杂度评估后）。
- 分支 `codex/relative-time-synthesis` 待 push + PR；之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-19] eval-result-grading：结果级评测 R1（12/12）

**做了什么**：
- 实现 change `eval-result-grading`（分支 `codex/eval-result-grading`）：
  - **ResultComparator**（`app/eval/result_comparator.py`）：按 intent 分层断言——exact（单值+容差）/ exact_per_key（带维度多行）/ trend_pattern（单序列 points + 多序列 series）/ top_set（集合+可选顺序）。
  - **R1 维度**：12 个结果可确定用例标 `expected_result`（真值用**独立于合成器的手工 SQL** 查库采集，记录 truth_source；P1 防把合成器 bug 烘焙进真值）。
  - **runner 审计执行**：R1 用真实 MySQL 独立执行系统合成 SQL（mysql CLI `-B` 解析列名）→ 断言；失败分类 sql_error/exec_error/value_mismatch；交叉诊断仅统计 value_mismatch。
  - mock 平台也可评 R1（执行层独立于系统）；relative 时间用例（c03 等）因系统时间未展开缺口暂不覆盖。

**测试**：
- Python pytest **160/160**（新增 result_comparator 8 + eval_runner R1 2）、ruff clean。
- **R1 评测（--llm real --platform mock，45 用例）**：可用性 100%、L1 96.97%（32/33，零回退）、**R1 12/12（100%）**、交叉诊断空（无 value_mismatch）。

**结果**：
- 结果级评测方法论跑通：R1 独立于系统（手工 SQL 真值 + MySQL 审计执行），12/12 建立基准。
- 诚实结论：当前用例集合成 SQL 与真值全一致；R1 是未来合成器/时间语义改动的回归保险（relative 时间未展开是已记录缺口，修后用 R1 验证）。
- 面试叙事：评测从语义层（L1-L4）到结果层（R1 真实执行数值）双闭环，seed 42 确定性真值可复现。

**待办/下一步**：
- 相对时间语义修复（c03 类）后 R1 扩展到 relative 用例。
- R1 扩展到全量用例（需评估歧义/detail 可断言性）。
- 分支 `codex/eval-result-grading` 待 push + PR；之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-19] multi-metric-synthesis：同源表多指标聚合（n01 解锁）

**做了什么**：
- 实现 change `multi-metric-synthesis`（分支 `codex/multi-metric-synthesis`）：
  - `synthesize()` 支持多指标：**全部 metrics 经 `_resolve_path` 后落在 metric_daily 列路径** 且 intent ∈ {aggregate, trend} → 单 FROM + 多 SELECT 列（每列 `agg_expr AS code`，SUM 包裹按统一 gb 判断）。
  - 约束（review P1）：事实路径多指标（trend+dims=[content] 路由到 user_behavior_fact，各指标 factEventFilter 不同会合成空结果 WHERE）显式降级；跨源（n02）/ranking/detail 多指标降级。
  - P2：metric_daily 的 category 维度无 spurious JOIN。

**Bug**：
- [修复] test_semantic_path `test_synthesis_failure_falls_back` 断言"多指标→fallback"已过期（n01 类同源多指标现可合成）→ 改用跨源多指标（completion_rate+engagement_rate）验证 fallback 路径，并新增同源多指标→semantic 用例。
- [排查] pytest 反复"卡住"根因 = 残留 uvicorn 服务器 + mem_test/mem_dbg 调试进程占资源/端口（多次 kill zsh 包装未杀 python 子进程）→ 彻底清理后恢复 4-5s 全量。

**测试**：
- Python pytest **150/150**（合成器新增多指标 5 + semantic_path 新增 1）、ruff clean。
- **--memory off N=45 回归**：可用性 100%、**n01 sql_source=semantic 且 L2 全对**（解锁成功）、n02 仍 fallback（边界正确）、L1 96.97%（与基线严格一致）、L2 81.82%（失分全为单指标用例 LLM 解析方差，历轮 81-91% 波动范围内）、端到端 97.78%、拦截 100%。

**结果**：
- 多指标 NL 查询从降级 raw SQL → 确定性语义合成（n01 关闭）；跨源（n02）诚实边界。
- 简历叙事：产品能力完整性补齐（"播放量+点赞量同查"是 ChatBI 常见需求）。

**待办/下一步**：
- 跨源表多指标（n02）子查询 JOIN 版（后续独立 change，需先确认 play_detail/fact 粒度可对齐）。
- 分支 `codex/multi-metric-synthesis` 待 push + PR；之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-18] metric-alias：指标表达映射 + 虚拟澄清实验

**做了什么**：
- 实现 change `metric-alias`（分支 `codex/metric-alias`）：
  - **别名表**（`app/eval/aliases.yaml`，17 个词组级别名 → 4 个指标码）：播放走势/播放趋势/播放表现/观看量/完播情况/点赞数/互动 等，最长匹配优先，每个别名标注覆盖用例。
  - **读取扩展**：`extract_metric_names`/`metrics_consistent`/`hit_allowed` 接别名（catalog 精确名优先 + 别名弱信号；`found==stored` 双保险保留；P3-1 最长匹配防「播放走势」被「播放」错配）。
  - **指标 ID 表达指纹**（可选增强，`MetricIdFingerprint`，默认 `MEMORY_ALIAS_FINGERPRINT=0` 关闭）：从 catalog + 沉淀表达按 metric_codes 归属构建每 ID 表达集；相似度定义写死 embedding cosine（difflib 降级）；`calibrate_fingerprint.py` 标定脚本。
  - **虚拟澄清实验**（`--virtual-clarify`）：歧义判定（低置信/多指标候选）+ golden 模拟完美用户选择 → 潜在澄清率（拆歧义且错/歧义但对）/ 虚拟澄清收益 / 澄清率随记忆下降（band 分层，miss 单独报记忆不可达）。

**Bug**：
- [修复] `--virtual-clarify` 分支顺序错误（synonym 分支先执行）→ 调整到 synonym 分支之前。
- [修复] `_clarify_decision` 缺 extract_metric_names import → 函数内补 import。
- [修复] 测试 ROOT 计算 parents[3] → parents[2]；`_entry` resolved_intent 缺 confidence 导致 intent_acceptable 失败。

**测试**：
- Python pytest **144/144**（新增 test_metric_alias 12 + test_virtual_clarify 5）、ruff clean。
- **4.2 --memory off N=45 回归**：可用性 100%、L1 96.97%（32/33）、L2 84.85%、L3 96.46%、端到端 95.56%、拦截 100%、命中率 0——**与 N=45 基线逐项完全一致，零回退**。
- **4.3 real-session 重跑**：**real_hit_rate 8/8**（7/8 → 8/8，c07 别名解锁）、real_consistency 8/8、real_persist_hits 8/8。
- **4.4 alias_hit**：cases 9→7、synonym easy 9→1、hard 13→3 匹配不到；band 分布 easy inject=6/hard inject=2。
- **虚拟澄清实验**：潜在澄清率 **0/35**、虚拟收益 **+0%**（LLM 全高置信且正确）——不做 HITL 的数据依据。

**结果**：
- 简历数字升级：真实路径命中率 7/8 → **8/8**（表达映射解锁 c07）；别名把 synonym hard 匹配缺口 13→3。
- 诚实结论：当前难度层 LLM 泛化饱和（澄清收益上限 0、难层真难层 0、注入增益 0 三线一致）；要测出记忆/澄清正确率增益需更对抗样本。

**待办/下一步**：
- 指纹增强标定验证（真实方舟 API，`calibrate_fingerprint.py`）后决定是否启用。
- 更对抗性改写（指标别名跨域术语/易混淆指标对）触发注入/澄清收益。
- 分支 `codex/metric-alias` 待 push + PR；之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-18] real-memory-baseline：真实路径记忆评测协议（real-session，实现完成，真实评测待 key）

**做了什么**：
- 实现 change `real-memory-baseline`（分支 `codex/real-memory-baseline`）：
  - runner 新增 `--protocol real-session`（默认 `eval`）+ `--namespace`（仅显式 default 联调开关）+ `--real-session-cases`。
  - `run_real_session_experiment`：8 个代表性问题会话 = 首问（沉淀）→ 二问（期望命中）→ 近似问（变体仅观测，band 任意分层）；mock 平台弱验证（close/reopen 文件持久化），real 平台强验证（重启服务器）留联调。
  - 指标口径分离：`real_hit_rate` / `real_consistency` / `real_persist_hits`（x/y 原始计数 + N=8 方向性标注）；`real_consistency` 复用重复对逐字段口径（intent/metrics/dimensions/time_range/filters/ordering）。
  - `_intents_equal` 逐字段一致判定；`_pick_real_sessions` 按 intent 多样性选会话；`_variant_for` 复用同义集 easy 层。
- 修复（memory-hybrid-retrieval 遗留）：`LanceVectorStore.init()` 对 lancedb>=0.37 的 `list_tables()` 返回 `ListTablesResponse` 对象未规范化 → 重开 store 时 `Table 'memory' already exists`；改为取 `.tables`。

**Bug**：
- [修复] lancedb 0.37 `list_tables()` 返回对象而非 list → `"memory" in tables` 恒 False → 重开建表冲突。
- [修复] `run_real_session_experiment` mock 平台漏 `init_graph(InMemorySaver())` → 补上。

**测试**：
- Python pytest **127/127**（新增 test_real_session 11 条：协议校验/namespace/会话选择/变体匹配/一致率/指标聚合/弱验证 SQLite+Lance/防污染）、ruff clean。
- **real-session 真实评测（--llm real --platform mock，8 会话）**：`real_hit_rate=7/8`、`real_consistency=8/8`、`real_persist_hits=7/8`（弱验证 close/reopen）、近似问 6 条观测。
- **c07 未命中（可归因）**：检索 1.0/hit 但 `metrics_consistent` 保守拦截——问题「播放趋势」不含 catalog 指标名「播放量」（字符串包含匹配）→ 不直通（走 LLM 且一致）。防毒化设计在真实路径起作用的证据，非缺陷。
- **5.2 回归（--memory off，N=45，real+mock）**：可用性 100%、L1 100%（33/33）、L2 90.91%、L3 97.98%、端到端 95.56%、拦截 100%、命中率 0——**相对 N=45 基线零回退**。
- 修复：real-session 实验强制 `settings.memory_enabled=True`（协议隐含 --memory on，默认 off 导致写/读路径跳过 → 命中 0）；`--llm mock` 校验；lance reopen 建表冲突（lancedb 0.37 list_tables 返回对象）。
- 真实评测曾被阻塞：DeepSeek `402 Payment Required`（key 欠费）→ 充值后恢复；方舟 embedding 正常（2048 维）。

**结果**：
- 协议实现完整（1.1-5.1 完成）；真实评测待 DeepSeek key 恢复。
- 简历口径：real-session 协议将产出"真实路径命中率基线"，与场内自命中（16-20%）分离。

**待办/下一步**：
- DeepSeek key 充值/恢复后：5.2 `--memory off` N=45 回归 + 5.3 real-session 真实评测（mock/real 平台）。
- 5.4 metrics-report「真实路径基线」章节（真实数据填充）+ 6.1 提交推送。

---

## [2026-08-18] eval-data-expansion：评测数据扩展 N=45 + 难层注入实验

**做了什么**：
- 实现 change `eval-data-expansion`（分支 `codex/eval-data-expansion`）：
  - **golden cases 25→45**：20 个新用例（multi_metric 2 / multi_filter 4 / ranked_time 4 / cross_table 4 / longtail_ambiguous 6），先真实 LLM 探测定稿（25 候选 23 PASS，n19"最受欢迎"采信 engagement_rate、n24 过泛丢弃）。
  - **同义集 20→35**：15 条难层（冷门叫法/间接说法/歧义/黑话/长尾），`difficulty` 分层（20 easy + 15 hard）。
  - **runner 难度分层**：by_difficulty 报告（easy/hard 的 N、组A/组B L1、注入增益）。
  - **hard_layer_filter.py**：组 A + 运行时 band → 三列判定表（真难层 = 组A错 且 band=inject，P1 双重条件）；真难层子集跑组 B → 逐例翻转表。

**Bug**：
- [修复] 标定测试因同义集扩容（20→35）断言过期 → 更新为 35。
- [修复] hard_layer_filter 未排序 import（ruff I001）→ --fix。

**测试**：
- Python pytest 116/116（同义集 35 校验、45 覆盖、classify 判定单测）、ruff clean。
- **N=45 全量基线（--memory off real+mock）**：可用性 100%、端到端 95.56%、L1 96.97%（32/33）、L2 84.85%、L3 96.46%、拦截 100%；**既有 25 例子集 L1/L2/L3 = 100%（零回退，P2-1 子集对比）**；新增 20 例 L1 95%/L2 75%（更难样本，独立报告）。
- **难层注入实验（真实 LLM + embedding）**：15 条难层候选组 A 无记忆 **15/15 全对** → 真难层 = 0 → 诚实报告"注入在该难度层无增益（模型泛化过强）"；双重条件筛选方法论验证成功（h05 落 inject 带但组A对 → 非难层）。

**结果**：
- 简历数字升级：N=45 更大样本基线（既有 25 例 100% 零回退 + 更难样本 L1 96.97%），可信度显著提升。
- 诚实方法论：难层"组A错 + band=inject"双重筛选证明可用（0 真难层也是可归因结论，非空转）。

**待办/下一步**：
- 结果级评测（eval-result-grading，`expected_result` 断言）——下一个 change。
- 触发注入收益需更对抗性难例（指标别名/跨域术语/模型易混淆指标对）。
- 分支 `codex/eval-data-expansion` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-18] memory-hybrid-retrieval：语义+词面多信号融合检索落地（LanceDB + 方舟 embedding）

**做了什么**：
- 实现 change `memory-hybrid-retrieval`（分支 `codex/memory-hybrid-retrieval`）：
  - **EmbeddingProvider**：火山方舟 doubao-embedding-vision（多模态端点 + 文本输入，2048 维），httpx 封装、可注入/mock、失败降级。
  - **LanceVectorStore**：LanceDB 嵌入式列式向量库（HNSW(cosine) + ICU 中文 FTS + WAL），同 MemoryStore 接口 + 向量/FTS 检索；写入同步 embed、失败不持久化；首条数据后惰性建索引。
  - **HybridRetriever（路径 B）**：精确快路径 + 向量/FTS 分开查 + D4 公式自算融合（w=0.7），embedding 不可用降级 difflib。
  - **阈值标定**（真实 API）：`calibrate_thresholds.py`，硬门槛 PASS——近重复 0.993≥hit_t、毒化 0.867<hit_t（+metrics 双保险）、同义 85%≥inject_t；定 **hit_t=0.92 / inject_t=0.82 / w=0.7**。
  - **runner 一致性修复**（eval-metrics review P1）：`_compute_synonym_bands` 改调真实检索器 + `hit_allowed` 共享判定（nodes/runner 同源，毒化→hit_rejected）。
  - **实验**：exp2/exp3 用 LanceDB + 真实 embedding，跑前探测 embedding 可用性（不可用标 DEGRADED）。

**Bug**：
- [修复] LanceDB 空表不能建向量索引（Not supported）→ 首条数据后惰性建索引；FTS 不支持 language=Chinese → 用 icu tokenizer。
- [修复] `_seed_memory` 静默吞错 → 补日志；HybridRetriever 暴露 best_score（标定用原始分布，miss 不再显示 0.0）。

**测试**：
- Python pytest 114/114（embeddings 5、vector_store 6、hybrid_retriever 6、calibrate 1、一致性 1 等新增）、ruff clean。
- **标定（真实方舟 API）**：同义集 20 条 fused 0.703-0.950，inject_t=0.82 时 85%≥inject_t；近重复 0.993、毒化 0.867。
- **exp2（同义注入，真实 LLM+embedding）**：**N_hit=0 / N_inject=8 / N_miss=12**——注入带从 difflib 的 0 变为可测量（样本守卫未触发）；组 A（无记忆）L1=100%、注入子集 L1=100%（增益 0，LLM 无记忆已泛化——预期内诚实结论）。
- **exp3（冷热，真实 LLM+embedding）**：N_hit=0 / N_inject=8 / N_miss=12（与 exp2 一致）；冷 L1=100% / 热 L1=100%（增益 0）；延迟 p50 冷 9.7s → 热 15.8s（注入示例加长 prompt → LLM 生成变慢，可解释）。
- **--memory off 全量回归（4.3）**：可用性 100%、L1 100%（13/13）、拦截 100%、命中率 0、端到端 92%（c17 已知 mock 用例）；L2 92.3% / L3 98.7%——唯一失分 **c04 ordering 单字段**（LLM 解析方差，与 memory-on 轮同用例同字段，memory off 下纯 LLM 解析，非本 change 引入）。

**结果**：
- 记忆系统从"命中直通 + token 省 21%"扩展到"**注入泛化可测量**"：N_inject 0→8，实验不再空转。
- 简历叙事：嵌入式向量库选型（LanceDB vs Milvus/Chroma/Qdrant 决策矩阵）、多信号融合检索（抄 Mem0 机制）、阈值标定方法论（近重复/毒化/同义三组对照）。

**待办/下一步**：
- 3.4 --memory off 全量回归（L1-L4 不回退）；4.1 pytest+ruff 终验；4.2 metrics-report 更新；4.3 本日志收尾。
- 后续：inject 示例对"更难改写"的收益（当前 LLM 无记忆已 100%）、sparse_embedding 词面信号升级、AST 骨架相似度。

---

## [2026-08-17] eval-metrics 落地：量化指标——token 计量 + 直通/注入/冷热实验

**做了什么**：
- 实现 change `eval-metrics`（分支 `codex/eval-metrics`，纯指标，不改业务行为）：
  - **Token 计量**：`app/clients/token_meter.py` 模块级 TokenMeter 单例 + `LLMClient._call` 记录 usage + runner 每 case 快照归因；报告新增 Token 总消耗/命中 vs 未命中均值。
  - **同义集**：`app/eval/synonym_cases.yaml` 20 条（5 类变换 × 4），含 golden_spec + source_case，**不静态标 band**（band = 沉淀后真实 retriever top-1）。
  - **三个实验**：`--synonym-cases`（组 A 无记忆 vs 组 B 有记忆，注入收益）、`--cold-hot`（检索侧冷热）、`run_repeat_pair` 逐遍计量（直通收益）；inject 子集 <8 显式标注样本不足；报告输出 N_hit/N_inject/N_miss。
  - **`docs/metrics-report.md`**：历史轨迹表 + 正确性/安全/效率/记忆价值 + 实验协议 + 局限说明 + 逐条审计四列表。

**Bug**：
- [修复] 同义/冷热实验未关闭内存 store → aiosqlite 连接残留进程不退出 → `_close_memory()` 收尾。

**测试**：
- Python pytest 95/95（新增 token 计量 4、同义集 2、直通收益 3）、ruff clean。
- **exp1（--memory on real+mock 全量 25 条，17:0x）**：可用性 100%、端到端 96%（仅 c17 已知 mock 用例）、L1 100%（14/14）、L2 92.86%（c04 ordering 单字段 LLM 解析方差，非记忆回归）、高风险拦截 100%、命中率 16%、Token 总消耗 66,117（命中均值 2,160 vs 未命中 2,737，省 ~21%）；重复对 c24 PASS（第二遍 memory_hit，intent 一致），但单对 token 差 -356/延迟差 -2,069ms——AnswerAgent 方差淹没解析阶段消除（N=1，需逐阶段计量）。
- **exp2（同义注入，真实 LLM）**：N_hit=0/N_inject=0/N_miss=20；组 A（无记忆）L1=100%；inject 子集 0<8 → 显式标注「样本不足，结论仅方向性」。根因：difflib 字符相似度对短中文问句 inject 带不可达（实测 0.455-0.778 全 miss）。
- **exp3（冷热，真实 LLM）**：冷/热 L1 均 100%、热命中 0/20、延迟 p50 10.8s→11.2s——与 exp2 互证，瓶颈在检索器非写路径。

**结果**：
- 简历可引用指标：端到端 47.6%→96%、L1 84.6%→100%、延迟 p50 49s→12.6s、高风险拦截 66.67%→100%、记忆命中直通 token 省 ~21%、同问同答一致率 100%。
- 诚实方法论产出：样本量守卫 + 波段分层揭示「字符相似度不适合中文短句近义检索」→ 明确下一阶段向量/词级检索方向。

**待办/下一步**：
- 直通收益逐阶段计量（语义解析 token 单独统计）——当前用例总 token 差被 AnswerAgent 方差淹没。
- 检索器升级（词级/向量相似度）后重标定阈值，使 inject 带可测量。
- 分支 `codex/eval-metrics` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-17] memory-namespace-isolation 落地：eval 记忆自隔离

**做了什么**：
- 实现 change `memory-namespace-isolation`（分支 `codex/memory-namespace-isolation`）：
  - **MemoryStore namespace**：表加 namespace 列 + (namespace, norm_question) 唯一键 + 存量迁移（默认 default）。
  - **记忆控制 API**：POST /internal/memory/seed（**拒 default** + intent 校验）、POST /internal/memory/clear（幂等）、GET /internal/memory/entries（X-Internal-Token）。
  - **Java 透传链**：AgentController @RequestParam memoryNamespace → EngineAnalyzeRequest → LangGraphClient。
  - **graph state + 读/写钩子按 namespace**；**runner 无论 on/off 都用 per-eval namespace**（eval-<date>-<ts>）。
  - **毒化反例**：seed {问题文本 X, intent metrics 不一致} → 查询 X（相似度 1.0）→ metrics 一致性校验拦截。

**Bug**：
- [修复] runner --memory off 时 golden cases 仍写进服务器 default 记忆（写钩子在服务器端不受 runner 控制）→ 污染真实记忆 → 改为**始终用 eval namespace**（off 也隔离到 eval namespace，default 零污染）。
- [修复] SCHEMA/MemoryEntry 的 namespace 字段落盘遗漏（脚本中断）→ 补全。

**测试**：
- Java mvn test 52/52（memoryNamespace 透传断言）；Python pytest 84/84（store 隔离/控制 API/毒化反例）、ruff clean。
- **--memory on 全量自隔离（15:06）**：可用性 100%、**命中率 20%**、c24 重复对 PASS、**c25 毒化反例 PASS（hit=False，metrics 校验拦截）**、L1 100%、端到端 96%、**default 零污染**。
- **--memory off 回归（15:25）**：**L1/L2/L3 100%**（不回退）、端到端 96%（仅 c17 已知 mock 用例）、**default 零污染**。

**结果**：
- 两个跨进程限制根治：反例真预置（毒化变体测 metrics 校验路径）、eval 记忆自隔离（无需 env/重启/删库，default 永不被污染）。
- 已知特征（如实记录）：--memory off 时服务器记忆仍开启，golden 集内相同/相似问题（c15/c16、c02/c23）会产生**场内自命中**（命中率 16-20%），但 L1-L4 经 spec_score 校验、不回退，且隔离到 eval namespace 不污染 default——"off 命中率 0"在 real 模式需服务端显式关闭记忆（MEMORY_ENABLED=false）才字面达成。

**待办/下一步**：
- （可选）/analyze 增加 memoryEnabled 每请求开关，使 --memory off 字面命中率 0。
- 记忆系统向量检索演进（Retriever 接口已预留）。
- 分支 `codex/memory-namespace-isolation` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-17] semantic-memory 落地：语义记忆——缓存直通 + few-shot 注入

**做了什么**：
- 实现 change `semantic-memory`（分支 `codex/semantic-memory`）：
  - **MemoryStore**（aiosqlite，零新依赖）：沉淀 `规范化问题 → ResolvedIntent`；catalog 校验（口径变更删除）+ resolver_hash 内容哈希失效（规则变更自动降级）。
  - **Retriever**：TextSimilarityRetriever（difflib，双阈值 0.95 直通 / 0.85 注入）+ metrics 一致性校验（防"点赞量"误命中"播放量"）。
  - **SEMANTIC_RESOLVE 记忆前置**：hit → 缓存直通（跳过 LLM，sql_source=memory，token 归零）；inject → few-shot 示例注入；miss → 现状。
  - **写钩子**：仅全链路成功沉淀；命中走 record_hit 独立路径。
  - **评测**：--memory off|on（默认 off 回归隔离）、重复对用例（一致率 100%）、相似反例、memory_hit/inject_rate 指标、memoryHit/Band 透传。

**Bug**：
- [修复] sql_synthesize 无条件覆盖 sql_source=semantic，冲掉 memory 来源 → 仅非 memory 时覆盖。
- [修复] memory 透传链：EngineAnalyzeResponse 缺 memoryHit/memoryBand → 加字段 + debug 透传 + run_real_case 解析。
- [修复] runner 收尾未关 MemoryStore → 进程不退出 → main 末尾 close。

**测试**：
- Java mvn test 51/51（含透传字段）；Python pytest 80/80（store/retriever/图记忆 13 个新用例）、ruff clean。
- **--memory off 全量回归（13:50）**：可用性 100%（25/25）、L1/L2/L3 **100%**（不回退）、命中率 0（隔离生效）、端到端 96%（仅 c17 已知 mock 用例）。
- **--memory on 聚焦冒烟**：c24 重复对 **PASS**（第二遍命中、intent 一致）、c25 反例**不命中**（metrics 一致性生效）、memory_hit_rate 可见。

**结果**：
- 记忆系统核心闭环：同问同答确定性（命中路径跳过 LLM）、相似语义不误命中、回归评测不受记忆影响。
- 已知限制（开发日志记录）：real 模式反例 seeding 写入 runner 本地 store 而非服务器 store（逻辑由单测覆盖，eval 级反例需服务端预置）；--memory on 全量 25 条需独立 MEMORY_DB_PATH。

**待办/下一步**：
- 记忆系统的向量检索演进（Retriever 接口已预留）、指标别名/用户偏好（非目标）。
- 分支 `codex/semantic-memory` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-17] semantic-dimensions 落地：语义层维度抽取优化

**做了什么**：
- 实现 change `semantic-dimensions`（分支 `codex/semantic-dimensions`）：
  - **根因基线（硬门槛）**：真实 LLM 探测 5 个失分用例 actual intent，实测推翻部分设计假设——主失败模式 = **date 误入 dimensions**（c07/c12/c13），c01/c03 为偶发；c07 的 filters IN 本就正确，对比类规则为 reinforcement。
  - **Prompt 优化**：date 禁止入 dimensions（属 granularity）、各分类→dimensions、类目限定→filters、对比类（dims + filters IN）、4 条正反示例。
  - **确定性兜底**（resolve() 内）：date 清洗（不强制补 day，按"每天/每日/按天"关键词触发）；"各分类"且空 → 补 category。
- 验证中修复：prompt 文件替换时误删收尾 `"""`+def 行（语法损坏）→ 修复。

**测试**：
- Python pytest 67/67（新增 5 个兜底单测）、ruff clean；Java 不受影响。
- real+mock 语义层评测（4.2）：L2 **100%**（61.54%→）、L3 100%、dimensions 失分 0。
- **real+real 完整评测（4.3，10:02）**：可用性 100%（23/23）、**L2 100%、L3 100%、L4 dimensions 100%、L1 100%**、端到端 95.65%（22/23，不回退）、高风险拦截 100%、auto_released 17.39% 意外拦截 0、延迟 p50 18.1s / p95 43.5s。唯一失败 c17（mock 设计用例，DQ 无截断无 "partial data"，预期残余）。

**结果**：
- L2 61.54% → 100%、L4 dimensions 61.54% → 100%——语义层维度抽取确定性达标，L1/L3/端到端零回退。
- 硬门槛（D1）验证了"先根因后修复"的价值：设计假设部分错误，实测纠正后主修复（date 清洗）恰好命中。

**待办/下一步**：
- c17 mock 用例真实模式期望（或标 mock-only）。
- 记忆系统实现（few-shot 检索 MVP，设计已就绪）。
- 分支 `codex/semantic-dimensions` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-14] eval-fidelity 落地：评测语义修正 + 意图层风险 + 回答兜底

**做了什么**：
- 实现 change `eval-fidelity`（分支 `codex/eval-fidelity`）：
  - **门禁 intent 入参**：`/internal/sql/validate` 接收 `intent` + `intentTimeRangeType`；`intent=detail` 且 time_range 缺失或 `type=none` → 无条件 `APPROVAL_NEEDED`（与 LLM SQL 形态无关，解决 c18 确定性拦截）；聚合意图豁免 LIMIT（修 c04/c10 误伤）；意图-形态一致性（聚合意图但 SQL 明细形态触碰 FACT → RETRYABLE）。
  - **评测语义**：risk 用例拦截即 PASS；非 risk 用例被拦截 → runner 自动调审批放行补跑（失败→ERROR）；报告新增 `auto_released` 计数/比例 + 意外拦截数。
  - **回答兜底**：LLM 回答 metrics 为空时用查询结果 `_basic_metrics` 填充；DQ 警告强制并入回答。
- 验证中修复：`event_type` 规则误伤 play_detail（该表无 event_type 列）→ 规则限定 user_behavior_fact；runner 自动放行 client 作用域 bug（async with 外调用已关闭 client）。

**Bug**：
- [修复] event_type 规则误伤：`SELECT AVG(completion_rate) FROM play_detail` 被 RETRYABLE（play_detail 无 event_type 列，重试无法修复）→ 规则仅作用于 user_behavior_fact。
- [修复] 自动放行 client 已关闭：`_approve_and_get` 在 `async with` 外调用 → RuntimeError → c04/c06/c10/c21 ERROR → 移入 client 上下文。

**测试**：
- Java `mvn test` 51/51（含 5 个意图规则用例）；Python pytest 62/62、ruff clean。
- 真实评测重跑（15:58，23 条）：**可用性 100%（23/23）、端到端 95.65%（22/23）**（重构前 73.91%）、L1 100%（13/13）、**L2 61.54%**（记录，dimensions 波动）、L3 92.31%、**高风险拦截率 100%**、**auto_released 17.39%（4/23）且意外拦截 0**、延迟 p50 12.8s / p95 30.2s。
- 关键验证：c04/c06/c10/c21 自动放行后 PASS；c18 确定性 WAITING_APPROVAL（`debug.resolvedIntent.time_range.type=none` 已核对）；c11 PASS（metrics 兜底生效）；唯一失败 c17 为 mock 设计用例（真实 DQ 无截断故无 "partial data"），属预期残余。

**结果**：
- 端到端从 73.91% → 95.65%，且每个失败可解释、门禁不过度拦截（意外拦截 0）可审计。
- 评测语义（自动放行）、意图层风险（detail 确定性）、回答兜底三项闭环。

**待办/下一步**：
- c17 mock 设计用例在真实模式的期望（或标记 mock-only）。
- 语义层 dimensions 抽取优化（L2 61.54%，另开 change）。
- 分支 `codex/eval-fidelity` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-14] sql-gate-unification 落地：统一门禁三态 + 语义层模型 + 图简化

**做了什么**：
- 实现 change `sql-gate-unification`（OpenSpec，分支 `codex/sql-gate-unification`，5 阶段里程碑提交）：
  - **语义层模型**：`TableSchemaRegistry`（解析 schema.sql → 12 表列注册表）、`TableType`（FACT/AGGREGATE/DIM）、`SensitiveColumns`（user_id 起步）——生成与门禁单源、零业务库依赖。
  - **SqlGateService 统一门禁**：`SqlGateResult` 三态（PASS/RETRYABLE/APPROVAL_NEEDED）；静态层 7 条 jsqlparser AST 规则（语法/SELECT/表存在/字段存在/明细 LIMIT+时间/敏感列含 SELECT \*/逻辑规则迁移）；计划层 `JdbcPlanAnalyzer`（EXPLAIN FORMAT=JSON，表类型感知：FACT 全扫/大扫描→审批、AGGREGATE/DIM 全扫→放行、TEMP/FILESORT 且 FACT→可重试）。
  - **图简化**：删除 SQL_VALIDATE 节点/边/字段，EXECUTE 直接承担成败判定；分类权威从 Python `_requires_human_approval` 挪进 Java；`SqlExecutionService` 收敛为只执行+熔断（D8 审批放行不变式）。
  - 退役：`SqlValidationService` / `SqlRulesChecker` / `sql-rules.yml` / 重复 SELECT/parse 检查。
  - 评测适配：c15/c16 期望对齐 gate 语义；新增 c22（事实全扫→审批正例）/c23（聚合全扫→PASS 反例）；runner mock 三态注入。

**Bug**：
- [新增修复] LLM 回答 `metrics` 偶发为 dict/标量 → Spring Jackson `Cannot construct instance of MetricPoint` → `/api/agent/analyze` 500（评测 3 个 ERROR）。→ 修复：`AnswerAgent._sanitize_metrics/_sanitize_charts/_sanitize_strings` 结构净化，LLM 输出形状不合规安全降级。

**测试**：
- Java `mvn test` 39/39（语义模型 6 + 门禁静态 14 + 计划层 5 + 执行契约 3 + 其他）；Python pytest 60/60、ruff clean。
- 真实评测重跑（11:42，23 条）：可用性 **100%（23/23）**、端到端 **73.91%（17/23）**（重构前 47.62%）、L1 100%、L2 53.85%、L3 91.03%、**高风险拦截 66.67%**（c22/c19 正确审批；c18 本轮 LLM SQL 恰好安全）、延迟 p50 **13.3s** / p95 29.6s（重构前 40s/91s）、自动修复 0 重试（语义 SQL 一次过）。

**结果**：
- 门禁从 5 组件冗余收敛为单一权威三态；validate/execute 不一致与聚合表全扫误报从构造上消除；SQL_FULL_SCAN→审批契约真正生效；端到端/延迟显著提升。
- 语义层模型底座就绪，为记忆系统模板（AST 归一化）复用铺路。

**待办/下一步**：
- 残余 LLM 不确定性：c04/c06/c10 走审批但等待报告缺字段（可考虑审批通过后补跑）；c18 拦截依赖 LLM SQL 形态；c11/c17 回答质量波动。
- 语义层 dimensions 抽取优化（L4=61.54%）。
- 分支 `codex/sql-gate-unification` 待 push + PR，之后 `/opsx:sync` + `/opsx:archive`。

---

## [2026-08-13] synthesizer-from-alias 落地：修复 BUG-006（合成器 FROM 别名缺表名）

**做了什么**：
- 实现 change `synthesizer-from-alias`（分支 `codex/synthesizer-from-alias`）：
  - `sql_synthesizer.py` 的 `synthesize()` 所有分支 FROM 子句改为 `FROM {source} {alias}`（聚合/趋势/排名 + detail；JOIN 场景沿用别名）。
  - 新增三 source 路径可执行性测试（metric_daily / user_behavior_fact / play_detail）；更新 `test_semantic_path` 与 `test_sql_synthesizer` 的 `FROM md`/`FROM ubf` 断言。
- 验证：修复后合成 SQL `SELECT ... FROM metric_daily md ...` 在真实 MySQL 上直接执行成功（修复前 `FROM md` 报 `Table 'md' doesn't exist`）。

**Bug**：
- [BUG-006]（修复）现象：真实评测 Source 列全 fallback，语义合成路径从未在真实库生效。原因：`_ALIAS` 生成别名引用但 FROM 只写别名未声明真实表名 → MySQL 报错 → 全量降级 raw LLM。修复：FROM 输出 `{source} {alias}`。

**测试**：
- Python pytest 59/59（新增三 source 路径测试）、ruff clean。
- 真实评测重跑（16:14，21/21 可用）：**Source=semantic 0/21 → 12/21**（c01/c03/c04/c07/c08/c09/c10/c12/c13/c15/c16/c21），semantic 用例 0 重试、延迟 2.8–31s；延迟 p50 40.5s→**30.2s**、p95 91.5s→**77.2s**；L1 保持 100%、L2 61.54%、L3 92.31%。
- ⚠️ 端到端成功率 66.67%→47.62%：主因 c15/c16 期望 `sql_retry_count=1`，但语义 SQL 一次通过护栏/DQ、0 重试 → 不符合预期（mock 注入用例在真实模式下的评测设计伪影，非真实回归）；c05 本次 LLM fallback SQL 触发审批（偶发）。

**结果**：
- 语义合成路径在真实库上真正生效：12/21 用例零 LLM 生成 SQL、延迟显著下降。
- 剩余待办（非目标）：c15/c16 评测设计（真实模式下重试数期望）、护栏误报/漏报（c04/c10/c21 误报、c18/c19 漏报）、dimensions 抽取（L4=61.54%）。

**待办/下一步**：
- 分支 `codex/synthesizer-from-alias` 待 push + PR。
- 可考虑调整 c15/c16 在真实模式的预期（语义 SQL 0 重试），避免端到端指标被设计伪影拖低。

---

## [2026-08-13] eval-harness-observability 落地：BUG-005 修复验证 + BUG-006 新发现

**做了什么**：
- 实现 change `eval-harness-observability`（OpenSpec，分支 `codex/eval-harness-observability`）：
  - `AnalysisReport` 增加 `status` 字段；`EngineAnalyzeResponse` 增加 `sqlSource`。
  - `AgentController`：正常路径置 `status=SUCCESS`，等待审批路径置 `WAITING_APPROVAL` 且 `includeDebug` 时组装 `debug{resolvedIntent, sqlRetryCount, sqlSource}`。
  - Python `AnalyzeResponse`/`routes.py` 透传 `sqlSource`；eval runner `run_real_case` 读取 status/resolvedIntent/sqlSource。
- 修复评测链路可靠性：`LangGraphClient` 引擎超时 90s→180s。
- 重跑真实评测两次（中间修超时），最终 21/21 可用。

**Bug**：
- [BUG-005]（修复）现象：等待审批用例的报告丢失 status/resolvedIntent → c18 误判、c04/c08 L1 误算 0。原因：Spring 等待路径提前 return，未透传。修复：等待报告带 status + debug。验证：L1 84.62%→**100%**（13/13），c04/c10 正确显示 WAITING_APPROVAL。
- [超时]（修复）现象：评测偶发 500（c17 两连 ERROR），`agent_run.total_duration_ms=90000` + `LangGraph request failed`。原因：Spring HttpClient 90s 超时 < 真实 LLM p95 120s。修复：90s→180s。验证：最终 21/21 可用。
- [BUG-006]（新发现，未修）现象：真实评测 Source 列**全部 fallback**。根因：确定性合成器产出 `SELECT ... FROM md ...`，**用了别名却没声明 `FROM metric_daily md`** → MySQL `Table 'md' doesn't exist` → 执行/验证失败 → 降级 raw LLM 并覆盖 sql_source。即**语义合成路径在真实库从未成功过**，全靠 raw LLM 兜底（L1=100% 说明语义解析本身是好的）。修复方向（待开 change）：`synthesize()` 的 FROM 子句改为 `FROM {source} {alias}`，约 1 行 + 补真实执行测试。

**测试**：
- Python pytest 58/58（新增 `run_real_case` 观测解析测试）、ruff clean；Java `mvn test` 16/16（新增等待路径 + EngineAnalyzeResponse 映射测试）。
- 最终真实评测（15:08）：可用性 100%（21/21）、端到端 66.67%（14/21）、**L1 100%**、L2 61.54%、L3 93.59%、L4 除 dimensions（61.54%）外全部 100%、延迟 p50 40.5s / p95 91.5s。
- c18/c19 本轮因 LLM 生成的 SQL 未触发审批而诚实 FAIL（status=SUCCESS expected=WAITING_APPROVAL）——观测已如实，护栏漏报属独立问题。

**结果**：
- 评测体系现在能如实观测审批状态、语义结果、SQL 来源；基线数字可信（L1=100%）。
- Source 列暴露 BUG-006（合成器从未生效），是最有价值的副产品。

**待办/下一步**：
- 开新 change 修复 BUG-006（合成器 FROM 别名），修复后 Source 列应显示 semantic，且端到端成功率/延迟应显著改善（省去 raw LLM 生成）。
- 护栏误报/漏报调查（c04/c10 误报、c18/c19 漏报）。
- 语义层 dimensions 抽取优化（L4 dimensions=61.54%）。
- 分支 `codex/eval-harness-observability` 待 push + PR。

---

## [2026-08-13] 真实 LLM 评测重跑（充值后）+ BUG-005 评测观测缺口

**做了什么**：
- DeepSeek 账户充值后重跑真实评测：`cd agent-engine && .venv/bin/python -m app.eval.runner --llm real --platform real`（21 条，13:37–13:59，约 22 分钟，全链路真实 LLM + MySQL）。
- BUG-003 修复验证通过：评测可用性 71%→**100%**（21/21），6 个原 ERROR 用例全部恢复（c07/c09/c12 等）。
- 通过比对 MySQL `agent_run` trace 与评测报告，发现并定位 BUG-005。

**Bug**：
- [BUG-005] 现象：真实评测报告 status 列与 MySQL trace 不一致（c04/c08/c18 实际 WAITING_APPROVAL，报告显示 SUCCESS）；且 c04/c08 的 L1 得 0 分。原因：Spring `AgentController.analyze` 对 WAITING_APPROVAL **提前 return 等待报告，丢弃了 status 与 debug.resolvedIntent**，eval runner 拿不到 → 误判。影响：c18 实际正确触发审批却被判 FAIL；c04/c08 的 L1 被误伤为 0（实为硬护栏误报，见下）。

**测试**：
- 21/21 无 ERROR；端到端成功率 57.14%（12/21）；**L1 84.62%（11/13）**、L2 53.85%（7/13）、L3 79.49%（judged=13）；L4：intent/metrics/time_range/filters/ordering 均 84.62%、**dimensions 53.85%**；延迟 p50 49.1s / p95 136.5s（模型本身慢）。

**结果**：
- 真实基线确立：L1 84.6%，语义主链路（语义解析→合成→护栏→执行→回答）可用。
- 新发现 3 类问题：
  1. **评测观测缺口（BUG-005）**：等待审批路径不透传 status/resolvedIntent。
  2. **硬护栏误报**：c04 完播率 / c08 日期区间 / c21 why 被判高风险（疑为合成 SQL 含子查询/全表扫描触发 SQL_LARGE_SCAN）。
  3. **硬护栏漏报**：c19 明细查询无时间范围未触发审批，直接执行。
  4. **语义层维度抽取最弱**：dimensions 53.85%，c01/c03/c07/c12 四个趋势用例漏 category 维度。

**待办/下一步**：
- 开新 change 修复 BUG-005：等待报告透传 `status=WAITING_APPROVAL` + `resolvedIntent`（Java + Python runner 两侧）。
- 调查硬护栏误报/漏报（c04/c08/c19 的合成 SQL 与 guard 判定规则）。
- 优化语义解析 prompt 的 dimensions 抽取（趋势/对比类问题）。

---

## [2026-08-13] BUG-003 修复（chartType 反序列化）+ 真实 LLM 首轮评测

**做了什么**：
- 修复 BUG-003：真实 LLM 生成的 charts 含 `chartType` 字段，Java `ChartConfig` DTO 只有 `type` 且未忽略未知字段 → `/api/agent/analyze` 500 `Unrecognized field "chartType"`。给 `ChartConfig`/`MetricPoint`/`DataPoint` 三个 DTO 加 `@JsonIgnoreProperties(ignoreUnknown = true)` 容错（保留 `type` 映射）。
- 单请求验证：原 500 的 c07（对比美食和游戏分类的播放趋势）现返回 HTTP 200，SQL/summary/charts 字段完整。
- `application.yml` 为所有必填占位符加默认值（DB `root/123456`、`AI_API_KEY:dummy` 等），支持 IDEA 一键启动；`AI_MODEL=deepseek-v4-flash`（用户手动指定，已保留）。
- 新增 `agent-engine/tests/conftest.py`：autouse fixture 强制 `eval_llm_mode=mock`，保证测试套件不因 `.env` 含真实 key 而发起真实 LLM 调用。

**Bug**：
- [BUG-003] 现象：LLM 输出 chart 含 `chartType` 字段 → Java 反序列化 500（`Unrecognized field`）。原因：`ChartConfig` DTO 仅声明 `type`，且无 `@JsonIgnoreProperties`。修复：3 个 DTO 加 `@JsonIgnoreProperties(ignoreUnknown = true)`。
- [BUG-004] 现象：`.env` 填入 `AI_API_KEY` 后 `pytest` 失败 1/57（`test_dq_warning_continues_to_answer`），warnings 多出 `Answer LLM fallback used: [Errno 8] nodename nor servname...`。原因：pydantic-settings 自动加载 `.env`，graph-flow 测试继承真实 key → AnswerAgent 在无网沙箱中真实调用失败 → 追加 fallback warning，破坏断言。修复：`tests/conftest.py` 强制 `eval_llm_mode="mock"`，`LLMClient.enabled()` 恒 False，恢复确定性 fallback。

**测试**：
- Python `pytest` 57/57、`ruff` clean；Java `mvn test` 14/14。
- 真实 LLM（deepseek-v4-flash）首轮 21 条评测（10:49）：评测可用性 71%（15/21）、端到端成功率 33.33%（5/15）、L1 口径核心 80%（8/10）、L2 严格 50%（5/10）、L3 平均字段 75%（judged=10）、L4 intent 80 / metrics 80 / dimensions 60 / time_range 80 / filters 80 / ordering 70；延迟 p50 43.4s / p95 97.8s（模型本身慢，单条 40–90s）。
- ⚠️ 该轮 6 个 ERROR（c07/c09/c12/c15/c16/c20）由 BUG-003 的 500 导致，修复后应计入 judged（L1 分母 10→13），需充值后重跑确认。

**结果**：
- 真实链路 500 根因（DTO 反序列化）已修复并单请求验证通过。
- 排查 `debug.resolvedIntent=null`：根因 = 语义解析的 LLM 调用失败（DeepSeek API 返回 **HTTP 402 Insufficient Balance**，账户欠费）→ 降级 fallback → `resolved_intent` 未写入。**非代码 bug，外部 API 欠费阻塞**。

**待办/下一步**：
- DeepSeek 账户充值后重跑 `cd agent-engine && .venv/bin/python -m app.eval.runner --llm real --platform real`（预计 10+ 分钟），验证 6 个 ERROR 恢复、L1 分母回到 13。
- 已知小缺口：`run_real_case` 未透传 `sql_source`，真实评测报告 Source 列恒为 `-`；后续可在 Python routes / Java debug 补透传。
- `codex/eval-real-hotfix` 分支待 push + 开 PR。

---
---

## [2026-08-13] MySQL 真实环境联调 + 端到端评测

**做了什么**：
- 用户本机安装 MySQL 8.0.46（Homebrew），配置 `root/123456` + `video_data_analysis` 库。
- 启动 Spring Boot（`SPRING_PROFILES_ACTIVE=dev`）：自动执行 `schema.sql` + `DataInitializer` 种子数据（7 指标 / 12764 事实事件 / 3713 播放明细 / 93 聚合行）。
- 启动 agent-engine（uvicorn:8090），新建 `agent-engine/.env`（`AI_API_KEY` 暂空）。
- 冒烟测试通过：Spring `/api/agent/analyze` → Python 引擎 → MySQL 真实 SQL 执行，全链路正常。
- 跑端到端评测 `--llm real --platform real`（21 条用例走真实平台）。

**Bug**：
- [BUG-001] `/internal/metrics` 返回 500：`MetricCatalogService.BASE_SQL` 的 SELECT 漏查 `fact_formula`/`fact_event_filter` 列，`map()` 读列时报 `Column 'fact_formula' not found`。→ 修复：SELECT 补上两列。（该 Bug 只有真连 MySQL 才会暴露，mock 模式跑不出来。）
- [BUG-002] eval runner `run_real_case` 两处：
  1. Spring 的 `debug.sqlRetryCount` 显式返回 `null` 时，`.get(key, default)` 不生效 → `int(None)` 崩溃，21/21 全 ERROR。
  2. Spring `/api/agent/analyze` 直接返回 `AnalysisReport`（无 `finalReport` 包装），runner 读报告字段全空。
  → 修复：`or 0` 兜底 + `or payload` 兜底。

**测试**：
- Python `pytest` 57/57、`ruff` clean、Java `mvn test` 14/14。
- 真实平台链路（fallback 模式，无 API key）：评测可用性 100%（21/21 无 ERROR）、端到端成功率 76.19%（16/21）、延迟 p50/p95 = 555ms/629ms。
- L1~L4 = 0%：无 API key 时语义解析不运行，属预期。

**结果**：
- MySQL 配置与真实平台链路完全验证通过；修复 2 个真实路径 Bug。
- 剩余 5 个失败均为无 API key 的预期行为：c15/c16/c17 依赖 mock 注入 guard/DQ 失败（仅 mock/replay 模式有意义）；c18/c19 需真实 LLM 生成高风险明细 SQL 才触发 HITL 审批。

**待办/下一步**：
- 在 `agent-engine/.env` 填入 DeepSeek `AI_API_KEY` 后重跑 `--llm real --platform real`，出真实 L1~L4 数字并验证 HITL/重试场景。
- 修复已提交到 `codex/eval-real-hotfix` 分支（含 `MetricCatalogService.java`、`runner.py`、`.gitignore`、`docs/开发日志.md`、`AGENTS.md` 日志规范）。

---

## [2026-08-12] eval-real-baseline 落地 + OpenSpec 清账

**做了什么**：
- 实现 `eval-real-baseline` change（26/26 任务完成，已合并 main）：
  - debug 透传：`/api/agent/analyze` 增加 `includeDebug`（默认 false），`resolvedIntent`/`sqlRetryCount` 可透传到评测端。
  - 失败隔离：runner 逐用例 try/except，环境性失败标 `ERROR` 不计入 judged，报告新增「评测可用性」。
  - 正交轴：`--mode` 拆为 `--llm mock|record|replay|real` × `--platform mock|real`，支持无 MySQL 的语义层评测。
  - 单数据源：新增 `src/main/resources/metric_catalog.json`（7 指标），Java 种子与 Python mock 同源，消除漂移。
  - 报告归档：`docs/eval-reports/eval-{llm}-{platform}-{eval_date}.json` + MD 最近快照；A/B 前校验配置一致。
  - golden 时间窗契约：语义 prompt 定死"未提时间 → none，禁止默认加窗"。
- OpenSpec 清账：sync 4 个已完成 change 的 delta 到主 specs，归档 4 个 change。

**测试**：Python 57/57、Java 14/14（新增 AgentController includeDebug 契约测试）、ruff clean、mock eval 跑通。

**结果**：4 个 commit（`25a52d0`/`f9a19cb`/`fb96d68`/`0b14409`）合并为 PR #4。

---

## [2026-08-11 ~ 12] 既有工作摘要（已合并 main）

- **legacy-cleanup**（PR #1）：下线全量图与旧 Java agent，收敛 ChatBI 主线。
- **langgraph-migration**（PR #2）：编排迁移到真 LangGraph（StateGraph + interrupt + SqliteSaver checkpoint）。
- **semantic-resolve-node**：语义解析（NL → ResolvedIntent）+ 确定性 SQL 合成 + `metric_definition` 指标字典。
- **agent-eval-harness**（PR #3）：golden_spec + 四层评分（L1~L4）+ FakeLLM 录制回放 + A/B 对比。
- **Redis 缓存移除**：删除 `SqlResultCache` 与 redis 依赖（时效性不可控、命中率低）。
- **记忆系统设计**：`docs/记忆系统设计.md`——分层记忆（few-shot 检索 / 指标别名 / 用户偏好），用 eval A/B 验收，暂不实现。
